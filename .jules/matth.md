## 2025-05-24 - Exact Hypergeometric Expectations for Lineage Coupling
**Learning:** Permutation testing for categorical co-occurrences without spatial dependencies (e.g. lineage coupling of clones across cell types) introduces non-deterministic variation, handles extreme tail values poorly, and is exceptionally slow. The probability of zero occurrences of a subset of cells assigned to a specific set of types within a randomly drawn clone of size $k$ perfectly maps to the hypergeometric distribution. Because calculating exact expectations and variances for single or paired presence involves evaluating probabilities of zero occurrences ($P(X = 0)$), we can use a computationally stable and fully vectorized log-gamma approximation ($P(X=0) = \exp(\ln\Gamma(N-K+1) - \ln\Gamma(N-K-k+1) - \ln\Gamma(N+1) + \ln\Gamma(N-k+1))$) and the inclusion-exclusion principle to perfectly solve for expectations and variances.
**Action:** Replace heuristic permutation testing with analytical log-gamma formulations of the hypergeometric distribution when evaluating categorical co-occurrence enrichment. It eliminates non-deterministic variation, provides exact variance estimates even in the extreme tails, and enables highly parallel vectorized calculations.

## 2025-05-25 - Exact Ordinary Least Squares for Exponential Decay
**Learning:** Fitting exponential curves ($y = a \cdot e^{bx}$) directly via non-linear least squares (e.g. `scipy.optimize.curve_fit`) relies on iterative numerical optimization. This is computationally slower, prone to local optima, and highly sensitive to initial parameter guesses and noise. Because the model is log-linear, we can apply a simple transformation ($\ln y = \ln a + bx$) to map the problem into standard Ordinary Least Squares (OLS), which has a closed-form analytical solution. This completely eliminates initialization heuristics and convergence failures, ensuring stable, exact extraction of physical attenuation parameters.
**Action:** Replace iterative numerical optimization with analytical OLS (`np.polyfit`) when fitting exponential decay models in Z-slice intensity correction. Ensure input intensities are strictly positive prior to the log transformation.

## 2025-05-25 - Architectural Rigidity and Statistical Modeling Separation
**Learning:** Hardcoding statistical fitting logic inside image processing loops creates brittle, non-extensible code and risks silent data corruption if fits fail. Furthermore, pure OLS on log-transformed data assumes multiplicative error in the original space, while Non-Linear Least Squares (NLLS) assumes additive error. Using OLS to seed NLLS guarantees convergence while maintaining the correct additive error assumption.
**Action:** Extract statistical modeling into a Strategy Pattern registry (e.g., `FIT_MODELS`). Consolidate redundant functions (`adjust_gamma_per_slice`, `adjust_brightness_per_slice`) into a single, rigorous pipeline (`correct_z_intensity_decay`). Enforce OLS-seeded NLLS for exponential models and fail loudly on fitting errors to prevent silent downstream propagation.

## 2025-05-25 - Multivariate Outlier Detection using Mahalanobis Distance
**Learning:** Cascading univariate constraints (like applying IQR or Z-score filtering independently across multiple dimensions) implicitly assumes orthogonal feature variance and destroys underlying covariance structures. This approach fails to detect outliers along non-axis-aligned directions and incorrectly trims valid tail values along the principal components. By reformulating outlier detection using the squared Mahalanobis distance ($D^2 = (X - \mu)^T \Sigma^{-1} (X - \mu)$) and scaling to precise Chi-Square asymptotic distribution bounds, we can robustly identify extreme multivariate outliers while perfectly preserving the intrinsic covariance geometry of the dataset. Using the pseudo-inverse (`np.linalg.pinv`) ensures stability even with collinear or rank-deficient dimensions.
**Action:** Replace naive cascaded univariate filters with multivariate Mahalanobis distance (`method='mahalanobis'`) when filtering structurally complex, high-dimensional datasets. Ensure the threshold is treated as a Chi-Square cumulative probability (e.g., 0.95 or 0.99) rather than an arbitrary multiplier.

## 2025-06-25 - Hierarchical Dispersion Fallback for Tied Distributions
**Learning:** Naive robust standardization methods that divide by the Median Absolute Deviation (MAD) fail catastrophically on zero-inflated or highly tied data because MAD collapses to zero. A common heuristic to fix this is silently replacing MAD with 1 or zeroing out the z-score, but this destroys variance estimates and masks valid outliers. A mathematically principled approach must use a hierarchical dispersion fallback: if MAD=0, fall back to the Mean Absolute Deviation (MeanAD) coupled with the Mean. If MeanAD=0, fall back to the Standard Deviation. To ensure scale consistency, these estimators must be multiplied by their asymptotic normal scaling factors (1.4826 for MAD, 1.2533 for MeanAD) rather than arbitrarily divided by an epsilon.
**Action:** Implement `robust_standardize` using the hierarchical dispersion fallback and use it in `remove_outliers` and other robust scaling utilities to guarantee mathematical stability on sparse or tied distributions without introducing unprincipled heuristics.

## 2025-06-25 - Vectorizing Jackknife BCa Bootstrapping
**Learning:** Calculating the bias-corrected and accelerated (BCa) bootstrap interval requires computing jackknife estimates, where each of $N$ samples is sequentially held out. A standard Python `for` loop to generate these $O(N)$ modified datasets and compute the statistic is terribly slow in Python, making BCa bootstrapping prohibitive for large $N$. We can fully vectorize this operation by constructing a 2D boolean mask (`~np.eye(N, dtype=bool)`), broadcasting the 1D dataset to $N \times N$, and slicing the masked values to extract all $N$ jackknife samples into an $N \times (N-1)$ array. We then use `np.apply_along_axis` to execute the statistic. This leverages C-optimized numpy loops and dramatically speeds up the acceleration factor calculation.
**Action:** Replace $O(N^2)$ scalar Python loops in bootstrap acceleration computations with a broadcasted 2D masked array structure, dramatically speeding up BCa estimation.
